#include<bits/stdc++.h>
using namespace std;

int main() 
{
    vector<vector<int>> matrix ;
    int rows , columns , intial_value = 0 ;
    vector<vector<int>> matrix(rows , vector<int> (columns, intial_value));

    int no_rows = matrix.size();
    int no_cols = matrix[0].size() ;

    return 0 ;

}
